// this is the model of the feature
// it's a type that has an array of form types

import { FormType } from '../../components/FormFactory/formModel';

export type FormBuilderType = {
  forms: FormType[];
};
